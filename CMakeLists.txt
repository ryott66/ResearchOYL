# cmake_minimum_required(VERSION 3.10)
# project(MyOpenCVApp)

# # OpenCVのパス（ここだけ自分の環境に合わせて）
# set(OpenCV_DIR "C:/opencv/build")

# find_package(OpenCV REQUIRED)

# add_executable(MyOpenCVApp main.cpp)
# target_link_libraries(MyOpenCVApp ${OpenCV_LIBS})
